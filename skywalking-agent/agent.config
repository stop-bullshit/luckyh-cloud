# SkyWalking Agent配置文件
# Agent服务名称（会被应用的spring.application.name覆盖） agent.service_name=${SW_AGENT_NAME:luckyh-cloud}

# Collector地址 collector.backend_service=${SW_AGENT_COLLECTOR_BACKEND_SERVICES:127.0.0.1:11800}

# 日志级别 logging.level=${SW_LOGGING_LEVEL:INFO}

# 采样率配置
# 负数表示不采样，0表示采样但不发送，正数表示每3秒采样N个 agent.sample_n_per_3_secs=${SW_AGENT_SAMPLE:-1}

# 忽略的后缀 agent.ignore_suffix=${SW_AGENT_IGNORE_SUFFIX:.jpg,.jpeg,.js,.css,.png,.bmp,.gif,.ico,.mp3,.mp4,.html,.svg}

# Span数量限制 agent.span_limit_per_segment=${SW_AGENT_SPAN_LIMIT:300}

# 认证token（如果OAP开启了认证） agent.authentication=${SW_AGENT_AUTHENTICATION:}

# 是否开启调试 agent.is_open_debugging_class=${SW_AGENT_OPEN_DEBUG:false}

# 实例属性 agent.instance_properties[organization]=luckyh-cloud agent.instance_properties[environment]=${SW_INSTANCE_ENV:dev}

# JVM参数配置 plugin.jvm.buffer_size=${SW_PLUGIN_JVM_BUFFER_SIZE:60}

# HTTP参数配置 plugin.http.http_params_length_threshold=${SW_PLUGIN_HTTP_HTTP_PARAMS_LENGTH_THRESHOLD:1024}

# SQL参数配置 plugin.sql.sql_parameters_max_length=${SW_PLUGIN_SQL_PARAMETERS_MAX_LENGTH:512}

# MongoDB追踪配置 plugin.mongodb.trace_param=${SW_PLUGIN_MONGODB_TRACE_PARAM:false}

# 缓存追踪配置 plugin.cache.redis.trace_redis_parameters=${SW_PLUGIN_CACHE_REDIS_TRACE_PARAMETERS:false}